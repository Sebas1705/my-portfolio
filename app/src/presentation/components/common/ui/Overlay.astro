---
interface Props {
    class?: string;
    style?: string;
}
const { class: className, style } = Astro.props;
---
<div class={`absolute inset-0 ${className ?? ''}`} style={style}>
    <slot />
</div>
