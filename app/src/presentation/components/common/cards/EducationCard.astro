---
import type { AcademicExperience, Language, CommonLabels } from '@/domain';
import { translate } from '@/domain';
import { formatDateRange } from '@/core';

interface Props {
    experience: AcademicExperience;
    lang: Language;
    commonLabels: CommonLabels;
}

const { experience, lang, commonLabels } = Astro.props;
---
<div class="bg-white/80 dark:bg-slate-900/70 rounded-xl p-6 shadow-md border border-gray-200 dark:border-slate-700 transition-transform duration-200 hover:-translate-y-1.5 hover:shadow-xl">
    <div class="flex flex-col md:flex-row md:justify-between items-center md:items-start gap-4 mb-4">
        <div class="flex-1 text-center md:text-left">
            <h3 class="text-lg font-bold text-primary mb-1">{translate(experience.degree, lang)}</h3>
            <p class="text-base text-slate-700 dark:text-slate-100 mb-1">{translate(experience.field, lang)}</p>
            <p class="text-base text-slate-500">{translate(experience.institution, lang)}</p>
        </div>
        <div class="flex flex-col items-center md:items-end gap-1 text-sm text-slate-500">
            <span class="font-semibold text-center md:text-right">
                {formatDateRange(experience.startDate, experience.endDate, lang, translate(commonLabels.current, lang))}
            </span>
            <span>{translate(experience.location, lang)}</span>
        </div>
    </div>
    <a href={`/${lang}/education/${experience.id}`} class="inline-block text-primary font-semibold mt-3 hover:text-primary-dark">
        {translate(commonLabels.viewMore, lang)} →
    </a>
</div>
