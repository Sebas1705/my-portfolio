import type { Translations } from "@/domain";

export interface CourseLabels {
    title: Translations;
    subtitle: Translations;
    pageTitle: Translations;
    viewCourse: Translations;
    certificate: Translations;
    skillsLabel: Translations;
}