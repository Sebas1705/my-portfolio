import type { Translations } from "@/domain";

export interface CommonLabels {
    viewMore: Translations;
    back: Translations;
    noData: Translations;
    current: Translations;
    code: Translations;
    demo : Translations;
}